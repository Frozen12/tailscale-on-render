services:
  - type: web
    name: tailscale-on-render
    env: docker
    autoDeploy: false

    envVars:
      - key: TAILSCALE_AUTHKEY
        sync: false
      - key: TAILSCALE_VERSION
        value: 1.94.2
      - key: ADVERTISE_ROUTES
        value: 10.0.0.0/8
